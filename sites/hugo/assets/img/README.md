# Site logo

Place an optimized logo in this directory and set its asset-relative path in
`config/_default/params.toml`, for example:

```toml
logo = "img/logo.svg"
logoAlt = "Site name"
```

Hugo processes this asset for the Blowfish header.

