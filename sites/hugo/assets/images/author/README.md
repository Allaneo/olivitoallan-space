# Author image

Place an optimized square image named `avatar.png` here, then uncomment
`params.author.image` in `config/_default/languages.en.toml`.

The image is processed by Hugo, so it belongs under `assets/`, not `static/`.

